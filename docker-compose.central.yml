# Central Server — chạy trên cloud/VPS
# docker-compose -f docker-compose.central.yml up -d

services:
  postgres:
    image: timescale/timescaledb:latest-pg16
    environment:
      POSTGRES_DB: stationos
      POSTGRES_USER: stationos
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - pg_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      Mode: Central
      ConnectionStrings__Postgres: "Host=postgres;Database=stationos;Username=stationos;Password=${DB_PASSWORD}"
      ASPNETCORE_URLS: http://+:5000
    ports:
      - "5000:5000"
    depends_on:
      - postgres

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.web
    environment:
      VITE_API_URL: ${BACKEND_URL}
    ports:
      - "80:80"

volumes:
  pg_data:
