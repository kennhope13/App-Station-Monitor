{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "thin-client",
  "description": "Capabilities for Station Monitor thin client (setup + navigate)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-emit",
    "core:event:allow-listen"
  ]
}
